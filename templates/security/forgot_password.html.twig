{% extends 'base.html.twig' %}

{% block title %}{{ 'forgot_password.title'|trans }}{% endblock %}

{% block body %}
    <div class="row">
        <div class="col-7 mx-auto">
            <div class="card border-secondary-subtle rounded-3">
                <div class="card-body px-4 pt-5 pb-3">
                    <h1 class="fs-4 fw-semibold mb-3">{{ 'forgot_password.title'|trans }}</h1>

                    {{ form_start(forgotPasswordForm) }}
                        <div class="mb-4">
                            {{ form_widget(forgotPasswordForm.email, {'attr': {'class':'form-control rounded-pill'}}) }}
                            {{ form_errors(forgotPasswordForm.email) }}
                        </div>

                        <div class="py-3">
                            <button type="submit" class="btn btn-primary rounded-pill fw-semibold">{{ 'forgot_password.send'|trans }}</button>
                        </div>
                    {{ form_end(forgotPasswordForm) }}
                </div>
            </div>
        </div>
    </div>
{% endblock %}
