{% extends 'base.html.twig' %}

{% block title %}{{ 'event.edit.title'|trans({'%title%': event.title}) }}{% endblock %}

{% block body %}
    {{ form_start(form) }}
        {% include 'event/_form.html.twig' with {
            'title': 'event.edit.title'|trans({'%title%': event.title}),
            'event': event
        } %}
    {{ form_end(form) }}
{% endblock %}
